#!/usr/bin/env bash
# @desc Select a file and browse text or JSON/YAML keys
set -euo pipefail
root="$(cd "$(dirname "$0")/.." && pwd)"
command -v python3 >/dev/null 2>&1 || { echo 'tbx cat: python3 required' >&2; exit 1; }
exec python3 "$root/lib/cat_browser.py" "$@"
